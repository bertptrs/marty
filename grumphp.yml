parameters:
    git_dir: .
    bin_dir: vendor/bin
    tasks:
        phpcsfixer:
            config: .php_cs
        phpunit: ~
        git_blacklist:
            keywords:
                - "die("
                - "var_dump("
                - "exit("
            triggered_by: [php]
